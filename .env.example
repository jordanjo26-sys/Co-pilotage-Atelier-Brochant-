# Copier ce fichier en .env et l'adapter.

# Base de donnees. Par defaut : SQLite local (aucune installation requise).
# Pour la production (OVHcloud), remplacer par une base PostgreSQL, par exemple :
# DATABASE_URL="postgresql://user:password@localhost:5432/copilote_brochant"
DATABASE_URL="file:./dev.db"

# Port d'ecoute du serveur HTTP
PORT=3000
